services:
  - type: web
    name: vret-wash-backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: SLACK_WEBHOOK_URL
        sync: false
